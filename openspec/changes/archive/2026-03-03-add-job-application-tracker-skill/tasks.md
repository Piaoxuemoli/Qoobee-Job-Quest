## 1. README 模板与记录结构落地

- [x] 1.1 在仓库根目录创建或更新 README，加入 `SUMMARY` 与 `RECORDS` 机器维护标记区块
- [x] 1.2 定义明细表头字段：ID、时间、公司、类型、岗位、进度、官网、备注
- [x] 1.3 在 README 中加入初始统计模板，覆盖总投递数、按类型统计、按进度统计、最近更新时间

## 2. Skill 对话与数据校验实现

- [x] 2.1 实现新增触发线：采集公司名、类型、进度、岗位、官网、时间并补问缺失项
- [x] 2.2 实现修改触发线：按 ID 优先定位，兜底使用公司+岗位+时间并处理多候选确认
- [x] 2.3 实现字段校验：时间格式、官网 URL、进度枚举合法性校验
- [x] 2.4 实现进度规则：支持非线性流转与“主动中止/拒绝”提前结束

## 3. README 自动更新与统计重算

- [x] 3.1 实现仅更新机器维护区块的写入逻辑，确保不改动区块外内容
- [x] 3.2 实现新增/修改后明细表重建或定点更新逻辑
- [x] 3.3 实现统计重算逻辑（按当前进度与类型聚合）并写入统计区块
- [x] 3.4 增加标记区块完整性检测与修复提示

## 4. Git 同步策略实现

- [x] 4.1 在每次 README 更新后增加“是否同步到 GitHub”显式确认
- [x] 4.2 实现同步命令链路：`git add`、`git commit`、`git push`
- [x] 4.3 统一提交信息模板：`docs(tracker): update application records [timestamp]`
- [x] 4.4 实现 push 失败处理：保留本地提交并输出失败原因与重试建议

## 5. 验证与使用文档

- [x] 5.1 编写最小验证清单：新增、修改、非法输入、多候选记录、拒绝同步、同步失败
- [x] 5.2 在 README 或相关说明中补充操作示例（新增对话、修改对话、同步对话）
- [x] 5.3 验证进度枚举为最终版本：已投递、测评、HR联系、笔试、一面、二面、三面、四面、面委会、HR面、Offer、拒绝、主动中止